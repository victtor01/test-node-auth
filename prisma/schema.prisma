generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id       String @unique
  name     String
  email    String @unique
  password String

  @@map("users")
}
